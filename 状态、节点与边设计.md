下面把你这个项目 **LangGraph 架构（含技能扩展）**，用最精简、可直接写进论文/文档的方式总结。

---

# One and Only 项目 · LangGraph 架构总结（最终版）

## 1. 核心思路
- 去掉人格参数、去掉 Embedding 风格向量，改用 **LoRA 微调模型** 控制语言风格。
- 情绪用 **Valence/Arousal/Control（VAC三维）** 表示。
- Live2D 动作：**VAC → 自训练小模型 → 动作参数**。
- 流程改为：**Plan → Execute → Judge**（执行后再校验）。
- 支持后续扩展 **Skills/工具调用**，不改动主链路。

---

## 2. 共享状态（State）
```
GraphState:
  user_input、memory、thought、
  need_skill、skill_name、skill_result、
  response_text、emotion_vac、action_params、
  judge_result、**need_judge（新增：是否需要校验）**
```

---

## 3. 节点设计（共7个）
1. **MemoryRetrieve**：检索相关历史对话
2. **Plan**：理解意图、生成思考、决定是否调用技能
3. **SkillRouter（扩展）**：分流，判断是否走技能
4. **SkillExecutor（扩展）**：执行技能并返回结果
5. **Execute**：
   - LoRA 生成回复文本
   - 输出 VAC 情绪
   - VAC → 动作模型 → Live2D 参数
6. **Judge**：校验文本、情绪、动作是否合理
7. **FinalOutput**：输出最终结果

---

## 4. 边与流程（可直接画图）
```
Start
  ↓
MemoryRetrieve（滑动窗口+少量召回）
  ↓
Plan（轻模型：意图+技能+是否需Judger）
  ↓
SkillRouter
  ├─ 需技能 → SkillExecutor（唯一主模型：文本+VAC+动作）
  └─ 无技能 → Execute（唯一主模型：文本+VAC+动作）
  ↓
if need_judge 
         ↓
       Judge
         ↓
     judge_result
     ↙          ↘
     true      false
     ↓           ↓
FinalOutput   Plan（重试1次）
else（闲聊无需校验）→ FinalOutput   async:summarize总结当前对话
  ↓
 End
```

---

## 5. 智能体分工(注：虽然是智能体但是有些智能体只会用到对话服务，因此在util当中有些是通过llm创建有些通过agent创建)
- **Planner**：思考、定方向、决策是否用技能
- **Executor**：生成文本、VAC、Live2D动作（LoRA+小模型）
- **Judge**：校验一致性，不合理则回退重规划
- **Finder**：查找记忆、提取相关信息
- **Summarizer**：总结当前对话、提取关键信息
---

## 6. 扩展原则（加技能/工具）
- **主链路不变**：Plan → Execute → Judge
- **新增节点插在 Planner 后、Executor 前**
- 新增技能只需在 **SkillExecutor** 内部扩展，不影响整体图结构
