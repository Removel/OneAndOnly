# Memory Manager Agent Configuration
model_config:
  model_name: "gpt-5"   # Model name for managing and retrieving information
  temperature: 0.3               # Low temperature for factual accuracy
  max_tokens: 2048               # Maximum tokens for response
api_config:
  base_url: "https://api.ssopen.top/v1"  # API endpoint
  api_key_env: "OPENAI_API_KEY"          # Name of environment variable containing the API key
agent_config:
  role: "Memory Manager"                 # Role of the agent
  description: "Manages memories and extracts relevant information"