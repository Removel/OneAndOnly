# 流式聊天功能快速开始指南

## 🚀 快速开始

### 1. 启动后端服务

```bash
# 在项目根目录下执行
python -m uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

### 2. 测试流式聊天

#### 方法 1: 使用测试脚本（推荐）

```bash
# 自动化测试
python test_scripts/backend_test/test_stream_chat.py

# 交互式示例
python test_scripts/backend_test/stream_chat_examples.py
```

#### 方法 2: 使用 cURL

```bash
curl -X POST http://localhost:8000/api/chat-stream \
  -H "Content-Type: application/json" \
  -H "Accept: text/event-stream" \
  -d '{
    "human_input": "你好，请介绍一下你自己",
    "session_id": 1,
    "clear_history": true
  }' --no-buffer
```

#### 方法 3: 使用 Python

```python
import requests
import json

url = "http://localhost:8000/api/chat-stream"
payload = {
    "human_input": "你好，请介绍一下你自己",
    "session_id": 1,
    "clear_history": True
}

response = requests.post(url, json=payload, stream=True)

if response.status_code == 200:
    for line in response.iter_lines():
        if line:
            decoded_line = line.decode('utf-8')
            if decoded_line.startswith('data:'):
                data_str = decoded_line[5:].strip()
                if data_str:
                    data = json.loads(data_str)
                    if 'content' in data:
                        print(data['content'], end='', flush=True)
```

## 📋 API 端点

### POST `/api/chat-stream`

**请求参数:**
```json
{
  "human_input": "用户输入内容",
  "session_id": 1,
  "clear_history": false
}
```

**响应事件:**
- `token`: 逐字输出
- `done`: 完成标记
- `error`: 错误信息

## 🎯 核心特性

✅ **实时流式输出** - 逐字显示 AI 回复（已验证：118 个 token 事件）  
✅ **SSE 协议** - 标准的服务器推送事件  
✅ **情绪分析** - 返回情绪 VAC 信息（已验证：valence: 0.3, arousal: 0.2, control: 0.7）  
✅ **会话管理** - 支持多轮对话  
✅ **错误处理** - 完善的异常处理  
✅ **向后兼容** - 保留原有 API  
✅ **异步支持** - 使用 AsyncSqliteSaver 和 aiosqlite  

## 🔧 修改的文件

1. [agent/main.py](file:///D:\Projects\One_And_Only\agent\main.py) - 添加流式聊天函数
2. [backend/service/ChatService.py](file:///D:\Projects\One_And_Only\backend\service\ChatService.py) - 添加流式服务方法
3. [backend/router/ChatRouter.py](file:///D:\Projects\One_And_Only\backend\router\ChatRouter.py) - 添加流式端点

## 📚 详细文档

- [流式聊天功能文档](file:///D:\Projects\One_And_Only\.harness\doc\流式聊天功能文档.md) - 完整的使用指南
- [流式聊天功能实现总结](file:///D:\Projects\One_And_Only\.harness\doc\流式聊天功能实现总结.md) - 实现细节和技术说明

## ⚠️ 注意事项

1. 确保后端服务已启动
2. 使用有效的会话ID
3. 首次对话建议设置 `clear_history: true`
4. 实现适当的错误处理
5. 设置合理的超时时间

## 🐛 故障排查

### 无法连接到服务器
- 检查后端服务是否启动
- 确认端口号是否正确（默认 8000）

### 没有收到事件
- 检查请求格式是否正确
- 确认 `Accept` 头设置为 `text/event-stream`

### 收到错误事件
- 查看错误消息获取具体信息
- 检查输入参数是否有效
- 查看服务器日志

## 📞 获取帮助

如有问题，请查看详细文档或联系开发团队。